---
description: Full tooling review of Justin's OWN infrastructure — inventory, verify live, fix, monitoring coverage, build/retire candidates. Excludes Mark/Skybox7 entirely.
---

Full tooling review for **Justin's own projects**. Standalone maintenance session.
Do not resume or interleave other work. Do not start anything the user asks for
mid-session until this finishes or they explicitly abandon it.

`$1`, if given, narrows the scope to one area (e.g. `ventures`, `vps2`, `monitors`).
No argument = everything below.

===========================================================
PHASE 0: SCOPE GATE
===========================================================

**In scope — Justin's estate only:**

| | |
|---|---|
| Servers | `vps1` 187.77.19.181 · `vps2` 187.124.65.189 · `vps3` 2.24.210.249 · `vps4`/`sentinel` 2.25.81.55 · `racknerd` 155.94.171.48 |
| Kuma | **https://status.willhitestrategy.org** — Uptime Kuma on **vps1**, behind Tinyauth SSO (`login.willhitestrategy.org`) |
| GitHub | `thejdubb02` |
| Code | `/root/platform` · `/root/ventures` · `/root/personal` · `/root/clients` · `/root/decommission` |
| Docs | `/root/platform/wsg-infra-audit` (the estate's forensic record) |
| Credentials | Vaultwarden at **https://vault.willhitestrategy.org** only — session token `/root/.bw-session`. Never `bitwarden.com`. |

**Out of scope, entirely:** anything under `/root/mark`, anything on the `mark`
/ `skybox7` box (2.25.76.180), the Duchamph GitHub org, Mark's Nextcloud
(`files.skybox7.com`), Mark's Vaultwarden entries, and **https://status.skybox7.com**.
Do not inventory it, do not touch it, do not report on it. If it surfaces, say
one line that it surfaced and move on.

**Confirm the target before touching Kuma.** Do not trust the URL alone — prove it:

```bash
ssh vps1 "sqlite3 -readonly -cmd '.timeout 5000' \
  /var/lib/docker/volumes/uptime-kuma-data/_data/kuma.db \
  'select count(*) from monitor;'"
```

Justin's instance lives in the volume `uptime-kuma-data` on **vps1** and carries
~19 monitors. **Mark's** lives in `uptime-kuma_uptime-kuma` on the `mark` box and
contains monitors named *Duchamp Hotel*, *Komodo (control)*, *Homepage (dashboard)*.
If you see those names, you are pointed at the wrong instance — **stop**. If the
query fails or you cannot positively identify which instance you reached, **stop
and say so**. Never continue against an unconfirmed target.

**The devbox is a workbench.** Nothing of Justin's should be living there
permanently — no production service, no only-copy of data, no unbacked-up state.
Flag anything that is.

===========================================================
SESSION RULES
===========================================================

- **Verify by running things live.** Never report status from docs, comments, a
  README, or memory. If you did not observe it working *this session*, say so in
  those words.
- **Never delete.** Propose removals and wait for approval.
- **No secrets in files, ever.** Vaultwarden item *names* as pointers only.
- **Update the docs in this same session** for any change you make — do not defer:
  - `wsg-infra-audit/MONITORING.md` — what is monitored, by what, and what a
    failure looks like. Create it if it does not exist.
  - `wsg-infra-audit/INFRASTRUCTURE.md` — what runs where. Create it if absent.
  - `wsg-infra-audit/CHANGELOG.md` — the hand-curated log of *intentional*
    changes. This is the most-read file in that repo; append to it.
  - A dated writeup in `wsg-infra-audit/audits/YYYY-MM-DD-tooling-review.md`.
  - Never hand-edit `inventory/` — that is machine-generated by `snapshot.sh`
    on VPS1.
- **Plain English.** Say what a thing does before how it works.
- Commit straight to `master`/`main`. No branches, no PRs.

===========================================================
PHASE 1: INVENTORY
===========================================================

List every **skill**, **slash command**, **CLI script**, **MCP server**, and
**scheduled job** in scope. For each: what it does in plain English, when it last
ran or was last used, where it lives, and whether it has a Kuma monitor.

Where to look, at minimum:

- Skills — `~/.claude/skills/`, `/root/platform/claude-kit/claude-config/skills/`,
  `<repo>/.claude/skills/` across `/root/platform` and `/root/ventures`
- Commands — `~/.claude/commands/`, `claude-kit/claude-config/commands/`
- Hooks — `agent-infra/tools/hooks/`, `~/.claude/settings.json`
- CLI scripts — `<repo>/tools/*.py` and `<repo>/scripts/*.sh`
- MCP — `/root/platform/mcp-gateway`, plus `agent-infra/tools/mcp_health.py`
- Scheduled jobs — `systemctl list-timers --all` and `crontab -l` on the devbox
  **and each of vps1/vps2/vps3/vps4/racknerd**; plus GitHub Actions in `thejdubb02`
- Monitors — the Kuma DB query above, joined against
  `agent-infra/llm-registry.yaml` via `agent-infra/tools/llm_map.py check`

Note last-run honestly: a timer that exists but has never fired is not "running".

===========================================================
PHASE 2: VERIFY
===========================================================

Exercise each item **for real**:

- Hit MCP health endpoints (`agent-infra/tools/mcp_health.py`).
- Run scripts with `--dry` / `--dry-run` / `--status` where one exists. Where none
  exists, say that, rather than assuming it works.
- Confirm each scheduled job's **last successful run** — `systemctl status`,
  `journalctl -u <unit> --since '7 days ago'`, or the job's own log.
- For skills and commands: confirm the file exists, parses, has valid frontmatter,
  and still points at paths, tools and hosts that exist.

**Flag anything running but doing the wrong thing.** A backup that completes while
writing somewhere nobody reads is a failure, not a pass. So is a job that exits 0
having processed zero records, a scraper whose source moved, a notifier pointed at
a dead webhook, and a health check that returns 200 from a static page.

===========================================================
PHASE 3: FIX
===========================================================

Fix what is broken. **Stop and tell Justin instead of working around it** when a
fix is destructive, needs a credential he has to rotate himself, or would change
behaviour of anything client-facing.

Anything given an LLM gets all three, no exceptions: its own capped `orkeys` child
key, an entry in `agent-infra/llm-registry.yaml` with a budget in
`/opt/or-keys/budgets.json`, and a Kuma monitor on vps1.

===========================================================
PHASE 4: MONITORING COVERAGE
===========================================================

**Say out loud, before touching it:** "Writing to https://status.willhitestrategy.org
— Justin's Uptime Kuma on vps1." If the target does not clearly resolve to that
instance, stop.

Everything that can fail silently needs a monitor:

- **Services** — HTTP check against a health endpoint that reflects *real* health.
- **Scheduled jobs** — a **push** monitor with a window slightly longer than the
  expected interval, so a job that stops running entirely gets caught. "It has no
  URL" is not an exemption.

Add what is missing. Kuma has no usable REST API for creating monitors — do it
through the UI in BrowserOS neo (Justin is signed in there; open your own tab,
close it when done). If you cannot reach it, **output the exact monitor config for
him to paste in and state plainly that it is not live yet.** Never report coverage
you did not confirm.

**Audit the existing 19 monitors for whether they check something real.** A monitor
on a page that returns 200 no matter what is worse than none, because it says
covered when it is not.

**Confirm the external watchdog on Justin's own Kuma is alive** — a dead monitoring
box looks exactly like everything being fine.

===========================================================
PHASE 5: BUILD CANDIDATES
===========================================================

Based on what Justin has actually been doing lately, recommend anything worth
turning into tooling. Preference order — **default to nothing**:

1. **Nothing.**
2. **CLI script** — deterministic, should run on a schedule without Claude.
3. **Skill** — you can already do it, but he keeps correcting you or you keep
   re-establishing the same context.
4. **Slash command** — a prompt he keeps retyping.
5. **MCP server** — only if you genuinely cannot reach a system and nothing
   existing covers it.

Only recommend if he would realistically use it again within a month. For each:
what it is in plain English, why that type, build time, ongoing maintenance cost,
and **how it would announce itself if it broke**.

**Do not build yet.** Wait for him to pick.

===========================================================
PHASE 6: DEPRECATION CANDIDATES
===========================================================

Propose removal for anything unused 60+ days, superseded, or costing more upkeep
than it returns. For each: what breaks if it goes, and what needs cleanup —
monitors, cron/timer entries, credentials, DNS, docs.

For anything he approves: **pause the Kuma monitor rather than deleting it, and
archive rather than delete.**

===========================================================
PHASE 7: REPORT
===========================================================

Open by confirming this covered **Justin's infrastructure** and naming the Kuma
instance you wrote to.

Then, short and plain: what is healthy · what you fixed · what is still broken and
why · what monitors you added · what to build · what to retire.

File anything left outstanding as a Nextcloud task on **his** server
(`nc_tasks.py`, list `Infra` unless a better one is obvious — ask, don't guess).
Close any task this session actually finished.

Close every BrowserOS neo tab you opened.
